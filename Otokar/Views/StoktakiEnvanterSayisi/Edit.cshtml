@model Otokar.Models.Data.StoktakiEnvanterSayisi

@{
    ViewBag.Title = "Envanter Düzenle";
}

<h2>Envanter Düzenle</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Stoktaki Envanter Sayısı</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)

        <div class="form-group">
            <div class="col-md-12">
                <label class="col-sm-2 control-label">Envanter Adı</label>
                <div class="col-sm-6">
                    @Html.TextBoxFor(m => m.EnvanterAdi, new { @class = "form-control" })
                </div>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-12">
                <label class="col-sm-2 control-label">Envanter Sayısı</label>
                <div class="col-sm-6">
                    @Html.TextBoxFor(m => m.EnvanterSayisi, new { @class = "form-control" })
                </div>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Kaydet" class="btn btn-success" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Listeye Geri Dön", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
